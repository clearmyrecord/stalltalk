# Quarantined legacy localStorage publishing files

Quarantined legacy static publishing files are retained for reference only and are outside runnable Next.js paths. Default issues are persisted in Neon through the `DefaultIssue` table.
