import Link from "next/link";
import { redirect } from "next/navigation";
import { createAdvertiserCampaign, signOutAction } from "@/lib/actions";
import { authEnvStatus, currentUser } from "@/lib/auth";
import { money } from "@/lib/format";
import { prisma } from "@/lib/prisma";
import { stripeEnvStatus } from "@/lib/stripe";

export const dynamic = "force-dynamic";
const SLOT_PRICE_CENTS = 5000;

function missingDb(error: unknown) {
  const message = error instanceof Error ? error.message : String(error || "");
  return /does not exist|P2021|AdSlotInventory|AdCampaign|User/i.test(message);
}
function badge(status: string) { return `rounded-full border-2 border-ink px-2 py-1 text-xs font-black uppercase ${["APPROVED", "PAID", "ACTIVE"].includes(status) ? "bg-green-100" : status === "REJECTED" ? "bg-stallRed text-white" : "bg-stallYellow"}`; }

export default async function AdvertiserPortalPage({ searchParams }: { searchParams: Promise<{ city?: string; venue?: string; month?: string; stripe?: string }> }) {
  const filters = await searchParams;
  const auth = authEnvStatus();
  const stripe = stripeEnvStatus();
  const user = await currentUser();
  if (auth.isConfigured && (!user || user.role !== "ADVERTISER")) redirect("/signin?error=role");
  const whereAdvertiser = user?.role === "ADVERTISER" && user.advertiserId ? { id: user.advertiserId } : {};
  try {
    const [advertisers, venues, inventory, campaigns] = await Promise.all([
      prisma.advertiser.findMany({ where: whereAdvertiser, orderBy: { name: "asc" } }),
      prisma.venue.findMany({ orderBy: [{ city: "asc" }, { name: "asc" }] }),
      prisma.adSlotInventory.findMany({ where: { status: "OPEN", ...(filters.month ? { month: filters.month } : {}), ...(filters.venue ? { venueId: filters.venue } : {}), ...(filters.city ? { venue: { city: filters.city } } : {}) }, include: { venue: true, restroom: true, qrCode: true, toiletLocation: true }, orderBy: [{ month: "asc" }, { slotNumber: "asc" }] }),
      prisma.adCampaign.findMany({ where: user?.role === "ADVERTISER" && user.advertiserId ? { advertiserId: user.advertiserId } : {}, include: { advertiser: true, inventory: { include: { venue: true, restroom: true, qrCode: true } }, payments: true }, orderBy: { createdAt: "desc" } })
    ]);
    const selectedAdvertiser = advertisers[0];
    const cities = [...new Set(venues.map((venue) => venue.city))];
    const months = [...new Set(inventory.map((slot) => slot.month).concat(["2026-07", "2026-08", "2026-09"]))].sort();
    return <main className="min-h-screen bg-paper p-4 text-ink md:p-8"><div className="flex flex-wrap items-center gap-3"><div><p className="font-black uppercase tracking-[.25em] text-stallRed">Advertiser login</p><h1 className="font-display text-7xl uppercase">Advertiser Dashboard</h1></div><form action={signOutAction} className="ml-auto"><button className="rounded-xl bg-stallRed px-4 py-3 font-black uppercase text-white">Logout</button></form></div><p className="max-w-4xl font-bold">Reserve paid Potty Favor inventory at {money(SLOT_PRICE_CENTS)} per spot, per month, per toilet/QR. Campaigns cannot publish unless payment succeeds and admin approval is complete when approval is required.</p>{!auth.isConfigured ? <Setup message="Set AUTH_SECRET and DATABASE_URL to restrict this dashboard to signed-in advertiser users." /> : null}{!stripe.isConfigured ? <Setup message="Set STRIPE_SECRET_KEY and NEXT_PUBLIC_SITE_URL to enable Stripe Checkout payment buttons." /> : null}{filters.stripe ? <p className="mt-4 rounded-xl border-4 border-ink bg-stallYellow p-4 font-black">Stripe status: {filters.stripe}</p> : null}<section className="mt-6 rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><h2 className="font-display text-5xl uppercase">Filter available inventory</h2><form className="mt-3 grid gap-2 md:grid-cols-4"><select name="city" defaultValue={filters.city || ""} className="rounded border-2 border-ink p-3"><option value="">All cities</option>{cities.map((city) => <option key={city}>{city}</option>)}</select><select name="venue" defaultValue={filters.venue || ""} className="rounded border-2 border-ink p-3"><option value="">All venues</option>{venues.map((venue) => <option key={venue.id} value={venue.id}>{venue.name}</option>)}</select><select name="month" defaultValue={filters.month || ""} className="rounded border-2 border-ink p-3"><option value="">All months</option>{months.map((month) => <option key={month}>{month}</option>)}</select><button className="rounded bg-ink p-3 font-black uppercase text-white">Apply filters</button></form></section><section className="mt-6 rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><h2 className="font-display text-5xl uppercase">Available locations & open slots</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{inventory.map((slot) => <article key={slot.id} className="rounded-xl border-2 border-ink bg-paper p-3"><p className="text-xs font-black uppercase text-stallRed">{slot.month} • Slot {slot.slotNumber} • {money(slot.priceCents)}/month</p><h3 className="font-display text-3xl uppercase">{slot.venue.name}</h3><p className="font-bold">{slot.venue.city}, {slot.venue.state} • {slot.restroom?.name || "Venue-wide"} • {slot.toiletLocation?.label || slot.qrCode?.code || "QR TBD"}</p></article>)}</div>{inventory.length === 0 ? <p className="mt-4 rounded-xl bg-stallYellow p-3 font-black">No open ad slot inventory matches these filters.</p> : null}</section><section className="mt-6 rounded-2xl border-4 border-ink bg-stallYellow p-5 shadow-brutal"><h2 className="font-display text-5xl uppercase">AI Creative Studio / upload & save draft</h2><p className="font-bold">Use the AI Creative Studio to generate copy/artwork, or paste an uploaded image URL below. Selecting multiple placements prices the draft automatically.</p><div className="mt-3 flex flex-wrap gap-2"><Link className="rounded bg-stallPurple px-4 py-2 font-black uppercase text-white" href="/admin/ad-studio">Open AI Creative Studio</Link></div><form action={createAdvertiserCampaign} className="mt-4 grid gap-3 md:grid-cols-2"><select name="advertiserId" defaultValue={selectedAdvertiser?.id || ""} className="rounded border-2 border-ink p-3">{advertisers.map((a) => <option key={a.id} value={a.id}>{a.name}</option>)}</select><input name="startsAt" type="date" className="rounded border-2 border-ink p-3" /><fieldset className="grid gap-2 rounded-xl border-2 border-ink bg-white p-3 md:col-span-2"><legend className="px-2 font-black uppercase">Select one or multiple placements</legend>{inventory.map((slot) => <label key={slot.id} className="flex gap-2 font-bold"><input type="checkbox" name="inventoryIds" value={slot.id} /> {slot.venue.name} • {slot.month} • {slot.restroom?.name || slot.qrCode?.code || "Venue-wide"} • Slot {slot.slotNumber}</label>)}{inventory.length === 0 ? <p className="font-black">No placements available to select.</p> : null}</fieldset><input name="name" placeholder="Campaign name" className="rounded border-2 border-ink p-3" /><input name="businessName" placeholder="Business name" required className="rounded border-2 border-ink p-3" /><input name="headline" placeholder="Ad headline" required className="rounded border-2 border-ink p-3" /><input name="creativeUrl" placeholder="Uploaded/generated creative image URL" className="rounded border-2 border-ink p-3" /><input name="targetUrl" placeholder="Website" className="rounded border-2 border-ink p-3" /><input name="ctaText" placeholder="CTA" className="rounded border-2 border-ink p-3" /><input name="months" type="number" min="1" defaultValue="1" className="rounded border-2 border-ink p-3" /><textarea name="body" placeholder="Offer/body copy" required className="rounded border-2 border-ink p-3 md:col-span-2" /><button disabled={!selectedAdvertiser || inventory.length === 0} className="rounded-xl bg-ink px-5 py-3 font-black uppercase text-white disabled:opacity-50 md:col-span-2">Save campaign draft</button></form></section><section className="mt-6 rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><h2 className="font-display text-5xl uppercase">Campaigns & previews</h2><div className="mt-4 grid gap-3">{campaigns.map((campaign) => <article key={campaign.id} className="rounded-xl border-2 border-ink p-3"><p className="flex flex-wrap gap-2"><span className={badge(campaign.status)}>{campaign.status}</span><span className={badge(campaign.approvalStatus)}>{campaign.approvalStatus}</span><span className="font-black">{money(campaign.priceCents)}</span></p><div className="mt-3 grid gap-3 md:grid-cols-[1fr_180px]"><div><h3 className="font-display text-3xl uppercase">{campaign.businessName}: {campaign.headline}</h3><p className="font-bold">{campaign.inventory?.venue.name || "Inventory TBD"} • {campaign.months} month(s) × {campaign.locationCount} location(s) • {campaign.paidAt ? "Paid" : "Unpaid"}</p><p className="mt-2 whitespace-pre-wrap font-bold">{campaign.body}</p>{campaign.rejectionReason ? <p className="mt-2 font-black text-stallRed">Rejection reason: {campaign.rejectionReason}</p> : null}</div><div className="rounded-xl border-2 border-ink bg-paper p-3 text-center"><p className="font-black uppercase">Preview</p>{campaign.creativeUrl ? <img src={campaign.creativeUrl} alt="Campaign creative preview" className="mt-2 aspect-square w-full rounded-lg object-cover" /> : <div className="mt-2 rounded-lg bg-stallYellow p-4 font-black">Creative pending</div>}<p className="mt-2 text-sm font-bold">{campaign.ctaText}</p></div></div><form method="POST" action="/api/stripe/checkout"><input type="hidden" name="campaignId" value={campaign.id} /><button disabled={campaign.status === "PAID" || campaign.status === "ACTIVE" || !stripe.isConfigured} className="mt-3 rounded bg-stallRed px-4 py-2 font-black uppercase text-white disabled:opacity-50">Pay with Stripe Checkout</button></form></article>)}{campaigns.length === 0 ? <p className="rounded-xl bg-stallYellow p-3 font-black">No campaigns yet. Select inventory and save a draft to begin.</p> : null}</div></section></main>;
  } catch (error) {
    if (!missingDb(error)) throw error;
    return <main className="min-h-screen bg-paper p-8 text-ink"><h1 className="font-display text-7xl uppercase">Advertiser Dashboard</h1><Setup message="Run Prisma migrations to create Phase 2B advertiser inventory tables." /></main>;
  }
}

function Setup({ message }: { message: string }) { return <p className="mt-4 rounded-2xl border-4 border-ink bg-stallYellow p-5 font-black shadow-brutal">{message}</p>; }
