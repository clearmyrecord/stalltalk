import { createRestroom, createVenue, deleteVenue } from "@/lib/actions";
import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";
export default async function VenuesPage() {
  const [publishers, distributors, venues] = await Promise.all([prisma.publisher.findMany(), prisma.distributor.findMany(), prisma.venue.findMany({ include: { publisher: true, distributor: true, restrooms: true, qrCodes: true } })]);
  return <section><h1 className="font-display text-7xl uppercase">Venues & Restrooms</h1><form action={createVenue} className="mt-4 grid gap-3 rounded-2xl border-4 border-ink bg-white p-4 shadow-brutal md:grid-cols-7"><select name="publisherId" className="rounded border-2 border-ink p-3">{publishers.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><select name="distributorId" className="rounded border-2 border-ink p-3"><option value="">No distributor</option>{distributors.map((d) => <option key={d.id} value={d.id}>{d.name}</option>)}</select><input name="name" placeholder="Venue" required className="rounded border-2 border-ink p-3"/><input name="slug" placeholder="slug" className="rounded border-2 border-ink p-3"/><input name="city" placeholder="City" required className="rounded border-2 border-ink p-3"/><input name="state" placeholder="State" required className="rounded border-2 border-ink p-3"/><input name="address" placeholder="Address" required className="rounded border-2 border-ink p-3"/><button className="rounded bg-ink p-3 font-black uppercase text-white md:col-span-7">Add Venue</button></form><div className="mt-6 grid gap-4 md:grid-cols-2">{venues.map((venue) => <article key={venue.id} className="rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase text-stallRed">{venue.publisher.name} • {venue.distributor?.name || "Direct"}</p><h2 className="font-display text-4xl uppercase">{venue.name}</h2><p className="font-bold">{venue.city}, {venue.state} • {venue.address}</p><p className="mt-2 font-black">{venue.restrooms.length} restrooms • {venue.qrCodes.length} QR codes</p></div><form action={deleteVenue.bind(null, venue.id)}><button className="rounded bg-stallRed px-3 py-2 font-black uppercase text-white">Delete</button></form></div><form action={createRestroom} className="mt-4 grid gap-2 rounded-xl bg-paper p-3 md:grid-cols-4"><input type="hidden" name="venueId" value={venue.id}/><input name="name" placeholder="Restroom name" required className="rounded border-2 border-ink p-2"/><input name="floor" placeholder="Floor" className="rounded border-2 border-ink p-2"/><input name="placement" placeholder="QR placement" className="rounded border-2 border-ink p-2"/><button className="rounded bg-ink p-2 font-black uppercase text-white">Add restroom</button></form><div className="mt-3 grid gap-2">{venue.restrooms.map((r) => <p key={r.id} className="rounded-lg border-2 border-ink bg-stallYellow px-3 py-2 font-black">{r.name} • {r.floor || "Floor TBD"} • {r.placement || "Placement TBD"}</p>)}</div></article>)}</div></section>;
}
