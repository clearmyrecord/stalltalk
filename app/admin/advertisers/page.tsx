import { createAdvertiser } from "@/lib/actions";
import { prisma } from "@/lib/prisma";

export default async function AdvertisersPage() {
  const [publishers, advertisers] = await Promise.all([prisma.publisher.findMany(), prisma.advertiser.findMany({ include: { publisher: true, ads: true, subscriptions: true } })]);
  return <section><h1 className="font-display text-7xl uppercase">Advertisers</h1><form action={createAdvertiser} className="mt-4 grid gap-3 rounded-2xl border-4 border-ink bg-white p-4 shadow-brutal md:grid-cols-6"><select name="publisherId" className="rounded border-2 border-ink p-3">{publishers.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="name" placeholder="Advertiser" required className="rounded border-2 border-ink p-3"/><input name="slug" placeholder="slug" className="rounded border-2 border-ink p-3"/><input name="contactEmail" placeholder="email" required className="rounded border-2 border-ink p-3"/><input name="portalNote" placeholder="portal note" className="rounded border-2 border-ink p-3"/><button className="rounded bg-ink p-3 font-black uppercase text-white">Add</button></form><div className="mt-6 grid gap-4 md:grid-cols-3">{advertisers.map((a) => <article key={a.id} className="rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><p className="text-xs font-black uppercase text-stallRed">{a.publisher.name}</p><h2 className="font-display text-4xl uppercase">{a.name}</h2><p className="font-bold">{a.contactEmail}</p><p className="mt-2 font-black">{a.ads.length} ads • {a.subscriptions.length} subscriptions</p></article>)}</div></section>;
}
