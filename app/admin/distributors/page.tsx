import { createDistributor } from "@/lib/actions";
import { prisma } from "@/lib/prisma";

export default async function DistributorsPage() {
  const [publishers, distributors] = await Promise.all([prisma.publisher.findMany(), prisma.distributor.findMany({ include: { publisher: true, venues: true, commissionReports: true } })]);
  return <section><h1 className="font-display text-7xl uppercase">Distributors</h1><form action={createDistributor} className="mt-4 grid gap-3 rounded-2xl border-4 border-ink bg-white p-4 shadow-brutal md:grid-cols-6"><select name="publisherId" className="rounded border-2 border-ink p-3">{publishers.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="name" placeholder="Distributor name" required className="rounded border-2 border-ink p-3"/><input name="slug" placeholder="slug" className="rounded border-2 border-ink p-3"/><input name="contactEmail" placeholder="email" required className="rounded border-2 border-ink p-3"/><input name="commissionRate" defaultValue="15" className="rounded border-2 border-ink p-3"/><button className="rounded bg-ink p-3 font-black uppercase text-white">Add</button></form><div className="mt-6 grid gap-4 md:grid-cols-3">{distributors.map((d) => <article key={d.id} className="rounded-2xl border-4 border-ink bg-white p-5 shadow-brutal"><p className="text-xs font-black uppercase text-stallRed">{d.publisher.name}</p><h2 className="font-display text-4xl uppercase">{d.name}</h2><p className="font-bold">{d.contactEmail}</p><p className="mt-2 font-black">{d.venues.length} venues • {d.commissionReports.length} commission reports</p></article>)}</div></section>;
}
